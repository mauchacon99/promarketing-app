export const classNameAccentContained = [
    "bg-accent-900",
    "text-primary-900",
    // Styles when component is in state HOVER
    "hover:bg-accent-500",
    // Styles when component is in state PRESS
    "pressed:bg-accent-500",
    // Styles when component is in state DISABLED
    "disabled:text-neutral-100",
    "disabled:bg-accent-100",
];

export const classNameAccentOutlined = [
    "bg-base-50",
    "text-accent-900",
    "border-2",
    "border-accent-900",
    // Styles when component is in state HOVER
    "hover:bg-accent-100",
    // Styles when component is in state PRESS
    "pressed:text-neutral-50",
    "pressed:bg-accent-500",
    // Styles when component is in state DISABLED
    "disabled:border-neutral-100",
    "disabled:text-neutral-100",
    "disabled:bg-base-50",
];

export const classNameAccentText = [
    "text-accent-900",
    // Styles when component is in state HOVER
    "hover:bg-accent-100",
    // Styles when component is in state PRESS
    "pressed:bg-accent-500",
    "pressed:text-neutral-50",
    // Styles when component is in state DISABLED
    "disabled:text-neutral-100",
    "disabled:bg-base-50",
];
